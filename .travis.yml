sudo: required 
language: node_js
node_js:
    - "stable"
    
services: 
    - docker
before_install: 
    - docker build -t wdong/sample-app -f Dockerfile.dev .
script:     
    - docker run -e CI=true wdong/sample-app npm run test -- --coverage --watchAll=false
